services:
  nginx-proxy:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: always
    ports:
      - 80:80
      - 81:81
      - 443:443
    volumes:
      - nginx-proxy-manager-data:/data
      - nginx-proxy-manager-letsencrypt:/etc/letsencrypt
    networks:
      - rother-odoo-proxymanager-network
  odoo:
    build:
      context: ./infrastructure/odoo
      dockerfile: Dockerfile
    container_name: rother_odoo
    restart: always
    #command: --dev all --log-level debug
    depends_on:
      - db
    volumes:
      - ./data/odoo/odoo-data:/var/lib/odoo
      - ./data/odoo/config-prod:/etc/odoo
      - ./data/odoo/addons:/mnt/extra-addons
      - ./data/custom_addons:/mnt/custom-addons
    environment:
      - HOST=db
      - USER=${POSTGRES_DB_USER}
      - PASSWORD=${POSTGRES_DB_PASSWORD}
    networks:
      - rother-odoo-internal-network
      - rother-odoo-proxymanager-network
      
  db:
    image: postgres:15
    container_name: rother_odoo_db
    restart: always
    volumes:
      - rother-odoo-db-data:/var/lib/postgresql/data/pgdata
      - ./data/db/shared_folder:/var/lib/postgresql/shared_folder
    environment:
      - POSTGRES_DB=${POSTGRES_DB_MAINTENANCE_NAME}
      - POSTGRES_PASSWORD=${POSTGRES_DB_PASSWORD}
      - POSTGRES_USER=${POSTGRES_DB_USER}
      - PGDATA=/var/lib/postgresql/data/pgdata
    networks:
      - rother-odoo-internal-network
  pgadmin:
    image: elestio/pgadmin:REL-8_10
    container_name: rother_odoo_db_admin
    depends_on:
      - db
    volumes:
      - rother-odoo-pgadmin-data:/var/lib/pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_LISTEN_PORT: ${PGADMIN_LISTEN_PORT}
    networks:
      - rother-odoo-internal-network
      - rother-odoo-proxymanager-network
volumes:
  rother-odoo-db-data:
  rother-odoo-pgadmin-data:
  rother-odoo-web-data:
  nginx-proxy-manager-data:
  nginx-proxy-manager-letsencrypt:

networks:
  rother-odoo-internal-network:
    driver: bridge
  rother-odoo-proxymanager-network:
    driver: bridge
  
